/** @type {import('next').NextConfig} */

const nextConfig = {
  images: {
    domains: ["localhost", "your-production-domain.com"], // Add multiple domains if needed
  },
};

export default nextConfig;
